# Agents — Implementation Reference

Status: stub. To be filled in as each agent is built (Phases 1-5).

Covers, per agent, once built: final prompt/instructions text and the reasoning behind it, output contract as actually implemented (vs. the PRD §6 sketch), model tier and why, any deviations from the PRD roster and why they happened. For the agent roster's design intent (roles, I/O types, model tiers, capabilities), see `PRD.md` §4 — that stays the source of truth for *intent*; this file is for implementation-level detail once agents exist.
