# Evals — Implementation Reference

Status: stub. To be filled in as each phase's eval dataset is built.

Covers, once built: where each agent's `Dataset` lives, naming conventions for `Case`s, which evaluators (deterministic/recall/judge) are wired to which agent and why, the `GroundingEvaluator` implementation, trajectory-eval setup for the Investigator (`HasMatchingSpan` queries), and the annotated-trace-to-eval-case workflow as actually practiced (PRD §9's flywheel). For the eval strategy's design intent, see `PRD.md` §8.
